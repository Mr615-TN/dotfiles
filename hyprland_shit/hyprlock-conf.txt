# Hyprlock Configuration for Debian Sid
# Doom One Colorscheme

background {
    monitor =
    path = ~/.config/hypr/wallpapers/lock-bg.png   # Fill in path to lockscreen background
    color = rgba(40, 44, 52, 1.0)
}

general {
    disable_loading_bar = false
    hide_cursor = true
    grace = 3
    no_fade_in = false
}

input-field {
    monitor =
    size = 200, 50
    outline_thickness = 2
    dots_size = 0.2
    dots_spacing = 0.2
    dots_center = true
    outer_color = rgb(81, 175, 239)
    inner_color = rgb(40, 44, 52)
    font_color = rgb(187, 194, 207)
    fade_on_empty = true
    placeholder_text = <i>Password...</i>
    hide_input = false
    position = 0, -80
    halign = center
    valign = center
}

label {
    monitor =
    text = Hi there, $USER
    color = rgb(187, 194, 207)
    font_size = 25
    font_family = JetBrains Mono Nerd Font
    position = 0, -180
    halign = center
    valign = center
}

label {
    monitor =
    text = $TIME
    color = rgb(187, 194, 207)
    font_size = 50
    font_family = JetBrains Mono Nerd Font
    position = 0, -20
    halign = center
    valign = center
}

label {
    monitor =
    text = Type to unlock
    color = rgb(81, 175, 239)
    font_size = 15
    font_family = JetBrains Mono Nerd Font
    position = 0, 30
    halign = center
    valign = center
}
